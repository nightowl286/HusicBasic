<UserControl x:Class="HusicBasic.Views.Dialogs.ConfirmationDialog"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:prism="http://prismlibrary.com/"             
             prism:ViewModelLocator.AutoWireViewModel="True">
    <Grid>
        <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBlock Grid.ColumnSpan="3" MaxWidth="300" TextAlignment="Center" Text="{Binding Message}" FontSize="14" Foreground="{DynamicResource Brush.Text}" TextWrapping="Wrap"/>

        <StackPanel Orientation="Horizontal" Grid.Column="1" Grid.Row="1" HorizontalAlignment="Center" Margin="0,15,0,0">
            <Button Style="{DynamicResource Button.Secondary}" Content="No" Padding="10,5" Margin="5,3" Command="{Binding GotResult}" CommandParameter="false"/>
            <Button Style="{DynamicResource Button.Primary}" Content="Yes" Padding="10,5" Margin="5,3" Command="{Binding GotResult}" CommandParameter="true"/>
        </StackPanel>
    </Grid>
</UserControl>
